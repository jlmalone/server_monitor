{
  "_comment": "Copy to ~/.config/server-monitor/secrets-sync.json. Hosts are SSH targets on the private overlay, never committed.",
  "sourceDir": "~/.config/secrets",
  "hosts": ["peer.example"],
  "exclude": ["redo-backups/"],
  "connectTimeoutSeconds": 12,
  "remoteNice": 19
}
